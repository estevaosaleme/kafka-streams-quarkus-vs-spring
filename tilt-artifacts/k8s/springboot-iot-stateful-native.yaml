apiVersion: apps/v1
kind: Deployment
metadata:
  name: springboot-iot-stateful-native
  labels:
    app: springboot-iot-stateful-native
spec:
  replicas: 1
  selector:
    matchLabels:
      app: springboot-iot-stateful-native
  template:
    metadata:
      labels:
        app: springboot-iot-stateful-native
    spec:
      containers:
        - name: springboot-iot-stateful-native
          image: springboot-iot-stateful-native:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 9404
          resources:
            limits:
              memory: "512Mi"
              cpu: "1"
---
apiVersion: v1
kind: Service
metadata:
  name: springboot-iot-stateful-native
spec:
  selector:
    app: springboot-iot-stateful-native
  ports:
    - protocol: TCP
      port: 9404
      targetPort: 9404
  type: ClusterIP 